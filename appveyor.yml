os: Visual Studio 2015

platform: x64
configuration: Debug
matrix:
  fast_finish: true

install:
  - choco install opencv -version 2.4.9.20140518

build_script:
  - mkdir build
  - cd build
  - cmake ..
  - cmake --build .
